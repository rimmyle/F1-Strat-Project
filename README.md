# Strategy Predictor

Simple Flask starter for a strategy predictor app.

## Run it

```bash
cd strategy-predictor
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Then open `http://127.0.0.1:5001`.
