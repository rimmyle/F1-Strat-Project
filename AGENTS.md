# Repository Guidelines

## Project Structure & Module Organization
This repository is a small Flask app. The main application logic lives in `app.py`. HTML templates are in `templates/`, with the main UI in `templates/index.html` and the error pages in `templates/error.html`. Static assets such as team logos and driver headshots live under `static/`. FastF1 data is cached locally in `.fastf1-cache/` and should not be committed.

## Build, Test, and Development Commands
- `python -m venv .venv` - create a local virtual environment.
- `.\.venv\Scripts\Activate.ps1` - activate the environment on Windows.
- `pip install -r requirements.txt` - install Flask and FastF1 dependencies.
- `python app.py` - start the app locally. It binds to `127.0.0.1` and defaults to port `5001` unless `PORT` is set.

## Coding Style & Naming Conventions
Use standard Python style: 4-space indentation, `snake_case` for functions and variables, and clear descriptive names for route helpers and graph builders. Keep template IDs and CSS class names consistent with existing patterns in `index.html`. Prefer small, targeted edits over broad rewrites because the app logic and UI are tightly coupled.

## Testing Guidelines
There is no automated test suite in the repository. Validate changes by running the app and checking the affected routes in the browser, especially `/`, `/session`, `/session-status`, `/results`, `/data`, `/lap`, and `/strategy`, plus any graph rendering or loading-state changes. If you add tests, place them alongside the relevant Python logic and name them clearly, such as `test_*.py`.

## Commit & Pull Request Guidelines
Recent commit history uses short imperative summaries like `Fix race overview and data selection` or `Update qualifying timeline and loading behavior`. Follow that style: concise, action-focused, and specific. Pull requests should explain the user-visible change, mention any data or cache implications, and include screenshots or short notes for UI updates.

## Configuration & Cache Notes
The app depends on FastF1 data and may behave differently when cache files are present. If debugging stale results, remove local cache contents under `.fastf1-cache/` rather than changing the source code first. Keep generated cache, log, and session artifact files out of commits unless they are intentionally part of the repo.

## Current App Behavior Notes
- `/` and `/session` are the session entry points, with `/session-status` polling the background loader.
- `/results` shows the race overview and qualifying tables/graphs once session data is ready.
- `/results` no longer includes the fixed session-results tower; the graphs now use the full main-page width.
- On race and sprint sessions, clicking a driver from `/data` now opens `/lap` directly; `/lap` remains the dedicated lap detail route.
- Clicking a stint opens `/lap` for that stint, and `/lap` defaults to a representative lap when only `driver` and `stint` are present.
- `/results`, `/data`, and `/strategy` now share the same session-view bootstrap helper so loading state, session badges, and fallback errors stay aligned.
- The non-qualifying driver picker in `templates/index.html` is collapsed to driver rows only; stint graphs now live in the lap view at the top of the page.
- Pit strategy graph stint bars now link into `/lap` with the selected driver and stint so the representative lap opens immediately on arrival, and practice sessions now reuse the same pit-stop overview graph on `/results` instead of leaving that card race-only.
- Race position driver clicks now route into `/lap` as well, landing on the top stint panel for the selected driver; the link now always carries a lap target so the click resolves to the lap view instead of a driver-only shell, and the visible driver line itself is now an actual SVG link target with a larger hit area while the status markers stay non-interactive.
- Session results rows in the race sidebar now open `/lap` for the selected driver and land on the top stint panel too.
- The lap page keeps the stint graph in the top panel beside the driver image/name and a compact stint-bar strip in the same left column stacked vertically instead of scrolling horizontally; those stint cards use wider minimum widths so the stint and lap-count labels stay on one line, the top panel now gives the graph more width again while keeping the bar cards readable, the driver headshot and driver/team text now stack vertically and the driver name/team block spans the rail so it centers over the pit stop badge, and the stint graph axis labels now show plain lap numbers on X plus `Lap` and `Time` axis titles while the Y-axis labels render as `m:ss.mmm` instead of raw seconds. The track map now sits in a single full-width support card with the primary telemetry and secondary telemetry flanking the map inside the same viewport pane so the gap-to-ahead chart stays visible, and the lap-record badges now live inline with the `Track map and telemetry` header rather than in a separate rail.
- The lap page stint dotline graph now badges out laps and in laps directly on the points, using `OUT`, `IN`, or a paired `OUT` + `IN` marker for combined pit-transition laps, and race or sprint stints now also show compact position-change markers over laps where the driver gains or loses places using signed deltas like `+1` or `-1` instead of `P+1`; those position markers now show a compact single-line hover pill with the abbreviated last names of the drivers that were passed or passed the driver, and the lap-time bubble only appears when hovering the lap dot while the driver-name pill only appears when hovering the position-change marker.
- The lap page stint dotline graph now distinguishes stint-best laps with a compact `SB` badge and the driver's best lap of the race with a gold `PB` badge, while the lap record pane no longer shows a separate `Personal Best` metric.
- The lap page stint stack now inserts a dedicated pit-time bar between consecutive stint cards so pit loss is visible directly in the left column, and the pit label uses a compact duration format without a leading zero minute when the stop is under one minute.
- The lap page pit-gap tooltip/title now tags the average comparison time with `(AVG)` so the hover text distinguishes it from the actual stop time.
- The lap page stint cards now show `(Fresh Tyre)` next to the stint number when the stint started on fresh rubber, and non-fresh stints now show the starting tyre age from `TyreLife` as a note like `(7 LAPS)`; the lap record pane no longer repeats that `Fresh Tyre` field as a separate metric.
- The hero-card lap summary badges are now vertically tightened so the value tiles sit flush instead of leaving extra bottom buffer, and the separate `FastF1 Generated` badge has been removed from the lap record metrics.
- The lap page pit-time bars now compare each stop against the average pit time of the other drivers in the race, and the badge now shows the pit stop time, the signed delta line, and the race-average pit stop time below it; if the stint cache is cold on `/lap`, the route now warms the missing race-driver stints from the current session before calculating that average so the comparison still shows up after a restart, the lap-panel grid keeps the pit stack and graph in separate contained columns so the pit UI does not crowd the graph, and the left lap-panel stack is centered as a narrower column inside its partition.
- The track map on `/lap` now uses a looser internal map margin and badge clamping so sector and speed markers keep more breathing room from the SVG edges and are less likely to clip, and the racing line itself is color-coded by acceleration and deceleration derived from the speed trace with a smoother percentile-based scale so the hue changes are more granular instead of reading as a hard binary. The separate track-map legend row has been removed from the card. The track map now lives in a single full-width support card, with the telemetry triplets stacked as a full-width row above the map and another full-width row below it, and the SVG height has been increased again so the map reads larger inside that viewport.
- The `/lap` track map now renders a dim circuit-surface layer underneath the animated racing line so the path reads as an actual track instead of only a floating telemetry trace, while keeping the acceleration-colored line on top. The surface path is now fitted against the real circuit SVG geometry when one loads, and the underlay is split into three sector-colored bands aligned to a cooler sky / blue / teal palette with small gaps between the bands so the map reads wider and easier to scan instead of as one flat outline; the track map itself is centered in the viewport between the sector badge rail and the telemetry columns.
- Sector badges on the `/lap` track map are now rendered as a stacked HTML column at the top-left of the map pane instead of floating over the SVG; the old hash/leader lines into the badge were removed.
- The lap marker on the `/lap` track map now uses the selected driver's team badge color for its ring and abbreviation text instead of the generic blue accent.
- The sector hash lines and sector badges on the `/lap` track map now share the same sector accent color so the track callouts read as one consistent element.
- The sector badges on the track map now normalize any lingering zero-padded duration string before rendering, so a 17.476-second sector reads as `17.476` instead of `00:00:17.476`.
- The `/lap` track-map sector markers now use the same darker summary-style badge language as the lap header metrics, with the sector label and split time stacked inside a single pill so the map callouts read more like the other lap badges, and the sector typography now uses higher-contrast outlined text for better readability. The lap-header summary badges also share a uniform minimum height so the row reads as an even strip.
- The `/lap` track-map card no longer shows an in-pane `Track map` section title; the telemetry rows and SVG now start immediately inside the card chrome, and the outer track-map viewport now uses a symmetric inset so the top buffer matches the left and right edges.
- The `/lap` page keeps the back action inside the stint hero card, while the `Track map and telemetry` header no longer shows its own back button; it still adds a `Back to overview` action only when the lap view was opened from the pit strategy graph or the race position graph, those graph links stamp the lap URL with a source marker so the overview return can be restored reliably, and the race-position driver lines now carry an explicit lap/stint URL so they land on the same lap state as the race data route.
- The race-position graph now renders Safety Car, Virtual Safety Car, and Red Flag markers in a dedicated band between the plotted lines and the x-axis labels so the indicators read cleanly under the chart instead of sitting on top of the scale.
- The pit-strategy graph now uses the same bottom-band treatment for Safety Car and Virtual Safety Car markers, keeping those status indicators between the stint bars and the x-axis labels instead of layered into the bar rows.
- The pit-strategy graph now normalizes the session time labels through the lap-time formatter and strips any lingering zero padding in the driver list display, so padded strings like `+02.256` or `+01:01.708` render without leading zeroes while whole-second values keep their `.000` precision.
- Telemetry on `/lap` now renders as two embedded card rows inside the track card: speed/throttle/brake above the map and RPM/gear/gap below it; each set is laid out as three smaller side-by-side cards with synchronized live carets, the telemetry chart x-axis labels and range text render as `m:ss` instead of raw seconds, the x-axis ticks now land on 15-second intervals, and the `Gap to Driver Ahead` trace is converted from distance to an estimated time gap in seconds while staying synced to the racing-line marker on the track map. The telemetry cards now measure their pane width at render time instead of using a fixed internal canvas, and the live value badges stay attached to each chart card instead of floating in a shared rail.
- On qualifying `/lap` pages, that gap trace is now rendered as `Gap to Fastest Lap` and is calculated against the fastest lap of the selected qualifying phase so the delta reads against the phase benchmark rather than an on-track car ahead.
- On race `/lap` pages, the `Gap Ahead` telemetry badge appends the currently-ahead driver's abbreviation in parentheses by sampling the `DriverAhead` telemetry channel and matching it against session results.
- The telemetry speed chart no longer overlays MAX/MIN callouts; the shared scrubber and per-row value pills are the only live indicators in that pane, and the telemetry chart carets now render as a line and dot only with no triangular tip.
- The hero-card lap metrics now render human-readable labels like `Green`, `Yellow Flag`, `Safety Car`, `Virtual Safety Car`, or `Red Flag` for track status instead of the raw FastF1 status code, and each supported status now glows in its own matching color in the lap record badge.
- The hero-card lap metrics no longer show the old compound or speed-trap fields for `Speed I1`, `Speed I2`, `Speed FL`, or `Speed ST`; the remaining lap-level fields stay in the compact badge cluster.
- The main page shell is intentionally wider than the original layout so the lap and results panes can use more horizontal space.
- The main page shell now animates on load-in and load-out so route changes fade and slide instead of snapping between views.
- Qualifying graphs use the chained phase logic from the run list, with Q1/Q2/Q3 windows of `18/15/12` minutes and sprint qualifying windows of `12/10/8` minutes.
- The shared qualifying timeline renderer keeps extra top spacing above each section header and pushes the first driver row lower so the section title and run labels do not crowd one another in `ALL`, `Q1`, `Q2`, and `Q3` views, the x-axis labels now show elapsed time from the displayed window start instead of the raw session clock, and the old shaded phase window indicator has been replaced with a compact top-of-graph `END Q1/Q2/Q3` marker made from two arrows instead of a full-height line.
- The qualifying timeline's `END Q1/Q2/Q3` phase marker now uses a brighter cyan glow so the arrow and badge stand out more clearly against the dark timeline background.
- The qualifying `/results` overview now pairs the qualifying timeline panel with the track-map view; each sector badge shows the fastest sector from the selected qualifying window, while the `/data` qualifying page keeps the driver list and lap details without rendering a standalone overview graph.
- The qualifying `ALL` overview timeline now fills the overview card width instead of shrinking to a fixed rendered pixel width, so the x-axis stays readable across the page.
- Clicking a driver in qualifying `/data` now opens `/lap` for that driver with the qualifying phase preserved and a representative lap auto-selected so the lap detail view is populated immediately.
- Practice sessions (`FP1`/`FP2`/`FP3`) now fall back to lap-derived driver rows when `session.results` is empty, which lets the practice overview render the pit-strategy graph and the practice driver list from lap data instead of hiding them.
- The qualifying `/lap` view now shows the selected run browser above the track map in the lap support panel; the active run shows all of its laps as clickable cards, the other runs from the same phase appear as selectable pills, and the route understands a `run=` query parameter alongside `lap=` so a run can be switched directly.
- Qualifying run anchors use pit-out timing for out-laps when available.
- 404 and 500 errors render the dedicated `templates/error.html` page.
